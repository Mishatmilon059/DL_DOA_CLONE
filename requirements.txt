# Tested working versions (verified in a clean Python 3.11 environment,
# including loading the pretrained ResNet/UNet .h5 weights with Keras 3).
# Minimum TensorFlow version per README_ORIGINAL.md is 2.10; this pin is
# the version this environment was actually validated against.
tensorflow==2.21.0
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.1
scikit-learn==1.9.0
tqdm==4.70.0
opencv-python-headless==5.0.0
